export const SignUpReducer = (store, action) => {
    switch ('') {
        case SIGN_IN: {
            return {
                ...state,

            }
        }

        default: {
            return state;
        }
    }
}

export const AC=()=>('')
