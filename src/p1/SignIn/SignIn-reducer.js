export const SignInReducer = (store, action) => {
    switch ('') {
        case SIGN_IN: {
            return {
                ...state,

            }
        }

        default: {
            return state;
        }
    }
}

export const AC=()=>('')